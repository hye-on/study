#include<iostream>

using namespace std;

int main()
{
	int N;
	cin >> N;
	int first = 1000000, last = 1,temp;
	
	for (int i = 0; i < N; i++)
	{ 
		cin >> temp;
		if (first >= temp) //제일 작은 수룰 찾는 것
			first = temp;
		if (temp > last) //제일 큰 수를 찾는 것
			last = temp;


	}
	
	cout << first * last;

}
