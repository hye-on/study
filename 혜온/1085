#include<iostream>

using namespace std;

int main()
{
	int x, y, w, h;
	cin >> x >> y >> w >> h;
	int a=x, b=y;
	if (w - x < x)
		a = w - x;
	
	if (h - y < y)
		b = h - y;

	if (a < b)
		cout << a;
	else
		cout << b;
}
